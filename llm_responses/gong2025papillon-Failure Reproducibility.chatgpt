```json
{
  "value": "High",
  "why": "PAPILLON generates adversarial inputs as text prompts (i.e., serialized inputs) via LLM-driven mutation operators and submits those prompts directly to target LLM APIs. The paper describes using an LLM helper to produce jailbreak templates, replacing placeholders with the harmful question to form the final prompt, and controlling tokenization/length (max_new_tokens). Because mutations produce final textual prompts (the serialized format actually used by deployed LLM systems) rather than small in-memory floating-point perturbations, there is no risk that fault-inducing changes will be lost by later serialization (rounding/clipping). Thus the method explicitly produces inputs in the serialized format relevant to the target, satisfying the metric’s requirement for reproducibility.",
  "evidence": "Page 8, Section 4.1 (Framework of PAPILLON): \"The jailbreak prompt is the final input to the LLM, created by replacing the placeholders (denotes as [INSERT PROMPT HERE]) in the jailbreaking template with the harmful question.\"; Page 9, Section 4.2 (Template Mutation and Optimization): \"PAPILLON uses a LLM (e.g., GPT-3.5 turbo), as a mutator to automatically create jailbreaking templates for specific questions.\"; Page 11, Section 5.1 (Experimental Setup): \"The length of the generated prompts is controlled using the max_new_tokens hyperparameter. Specifically, for the 'role-play' and 'contextualization' mutations, this value is set to 200 tokens...\""
}
```