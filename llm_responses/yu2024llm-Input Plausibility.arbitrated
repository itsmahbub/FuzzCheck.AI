```json
{
  "value": "High",
  "why": "LLM-Fuzzer mutates only the surrounding jailbreak template while always inserting the (semantically critical) target question intact. At each iteration, the template is altered via LLM-assisted operators that produce coherent, human-like text, ensuring naturalness. By fixing the question and only varying the template, the fuzzer prevents semantic drift of the critical content across iterations, directly matching the “High” criterion of fixing semantically critical content and preserving naturalness throughout the fuzzing process.",
  "evidence": "Page 3, Section 3.1 (“Subsequently, it combines this newly generated template with a target question to form a prompt.”) shows that the question is held fixed. Page 7, Section 3.3 describes the five LLM-based mutation operators (Generate, Crossover, Expand, Shorten, Rephrase) all designed to produce semantically coherent, natural text. Figure 3 illustrates that only the template is changed while the placeholder for the question remains constant."
}
```