```json
{
  "value": "High",
  "why": "SneakyPrompt constructs adversarial inputs as discrete, serialized text prompts by replacing sensitive tokens with other tokens from a token vocabulary (e.g., the CLIP token vocabulary) or from an English-word dictionary. Mutations operate on token-level text (strings) that are sent directly to the target model (OnlineQuery(pa,M)), so they are produced in the target serialized format (text) rather than as continuous/normalized intermediate representations. Therefore perturbations are not lost by later serialization/rounding and failures should reproduce under realistic input serialization.",
  "evidence": "Page 5, Section 4.1 Overview: \"The key idea of SneakyPrompt is to replace each sensitive token in pt as m non-sensitive tokens (called replacing tokens) to construct an adversarial prompt pa.\" \n\nPage 5, Section 4.1 (paragraph describing search space): \"Suppose D is the token vocabulary, e.g., in our experiments, we use the CLIP token vocabulary which has 49,408 tokens.\" \n\nPage 6, Step (3) in the Overall Pipeline: \"F(M, pa), M(pa) = OnlineQuery(pa,M). SneakyPrompt queries the online text-to-image model M with the generated prompt pa from Step (2), and obtains the returned safety filter result F(M, pa) and the generated image M(pa) if any.\" \n\nPage 8, Section 5 (Safety Filters / Non-English Word Safety Filter): \"Note that the search space of SneakyPrompt will be the google-10000-english dictionary [51] that contains a list of the 10,000 most common English words, instead of all tokens from CLIP vocabulary dictionary [52].\""
}
```