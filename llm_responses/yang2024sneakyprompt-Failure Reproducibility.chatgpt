```json
{
  "value": "High",
  "why": "The paper constructs adversarial inputs as discrete, serialized text prompts by replacing sensitive tokens with valid tokens from a vocabulary (e.g., the CLIP token vocabulary or an English word list) rather than applying small floating-point perturbations to internal continuous representations. The method explicitly samples replacement tokens from a token vocabulary Dl (CLIP tokens or an English word list), constructs a serialized prompt pa by substituting those tokens into the original prompt, and then queries the target model with that prompt string (OnlineQuery(pa, M)). Because the mutated inputs are valid serialized text prompts, they respect the discrete input format of deployed systems and will survive standard serialization/rounding/clipping—meeting the metric’s requirement that inputs be generated directly in serialized formats or explicitly enforce input-format constraints.",
  "evidence": "Page 902, Section 4.1 Overview: \"The key idea of SneakyPrompt is to replace each sensitive token in pt as m non-sensitive tokens (called replacing tokens) to construct an adversarial prompt pa.\"; Page 902, Step (2) of the pipeline: \"pa = Sample(pt, S). The function Sample obtains nm replacing tokens C = (c1, c2, · · · , cnm) from the search space S ... and replaces the sensitive tokens in pt as the replacing tokens to construct an adversarial prompt pa.\"; Algorithm 1 (SneakyPrompt-RL): \"pa ← Construct adversarial prompt based on C and pt\" and \"F(M, pa), M(pa) ← OnlineQuery(pa, M)\" (shows the constructed prompt pa is serialized and sent to the model).; Page 905, Non-English Word Safety Filter description: \"Note that the search space of SneakyPrompt will be the google-10000-english dictionary [51] that contains a list of the 10,000 most common English words, instead of all tokens from CLIP vocabulary dictionary [52].\""
}
```