```json
{
  "value": "High",
  "why": "TACTIC restricts test-input generation to the style space of a generative image-to-image translation model (MUNIT) trained on real driving-scene images, and it synthesises each mutated test image by recombining the original content with a sampled/optimized style from that learned distribution. Thus the search is confined to a plausible input distribution produced by a generative model trained to produce realistic inputs, and mutations are applied by mapping original content + style -> synthesized image (rather than repeatedly mutating already-synthesised images), preventing uncontrolled cumulative semantic degradation.",
  "evidence": "Page 2, Introduction: \"...TACTIC employs the Multimodal Unsupervised Image-to-Image Translation (MUNIT) (Huang et al., 2018) to deep-learn the features of environmental conditions from a set of sample scenes belonging to the same environmental type and encode them as a high-dimensional vector space called style space.\" (Section: Introduction)\n\nPage 3, Section 3.1 (Environmental Conditions): \"One insight of MUNIT is that the representation of images can be decomposed into a content space that is domain-invariant and a style space that captures domain-specific characteristics... Therefore, we can use the style space of an environmental type in a well-trained MUNIT model to represent the environmental condition space of the type.\"\n\nPage 3, Section 3.1 (Transformation process): \"To transform a driving scene x1 in X1 to a driving scene x2 in X2, the MUNIT model M first uses the encoder E1 of X1 to decompose x1 into a content vector c1 and a style vector s1... Then, to produce x2, M recombines the content c1 with a style vector s2 sampled from the style space S2 of X2 by the decoder G2 of X2, i.e., x2 = G2(c1, s2).\"\n\nPage 6, Section 3.3 (Search of Critical Environmental Conditions): \"TACTIC iteratively calls the (1+1) ES ... The initial individual, which is a style vector randomly sampled from the style space in the MUNIT model M... a new individual s' is firstly generated based on the current individual s. Then, the fitness values of s and s' is evaluated... the individual with higher fitness value is kept... the current individual is added to the set S as a critical environmental condition.\" (shows search is over MUNIT style vectors)\n\nPage 7, Section 4.3 (Image Quality / user study): \"We conduct a user study to evaluate the realism of the testing driving scenes synthesised by TACTIC... The results demonstrate that the testing driving scenes synthesised by TACTIC are more realistic than the ones synthesised by affine image transformation (e.g., DeepTest) or high-fidelity simulation (e.g., PreScan) and can better reflect real environments.\""
}
```