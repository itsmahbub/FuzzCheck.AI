```json
{
  "value": "High",
  "why": "AutoDAN operates directly on discrete text prompts (serialized text strings) rather than continuous or normalized representations. The method’s mutations / crossovers are implemented at paragraph, sentence and word levels (sentence/word replacements, synonym swaps, LLM-based rewriting), and population initialization and mutation explicitly use an LLM to produce revised sentence strings (Algorithm 5). The paper therefore generates final inputs as natural-language prompt strings (the same serialized format used in real deployments), so perturbations are preserved under serialization and failures should be reproducible in realistic settings.",
  "evidence": "Page 2, Introduction: \"Based on the aforementioned insights, we propose AutoDAN, a hierarchical genetic algorithm tailored specifically for structured discrete data like prompt text.\"; Page 5, Sec. 3.2 Population Initialization: \"To preserve the basic features of the prototype handcrafted jailbreak prompt while also promoting diversification, we employ LLMs as the agents responsible for revising the prototype prompt, as illustrated in Alg. 5.\"; Appendix B (Page 15), Algorithm 5 LLM-based Diversification: \"function DIVERSIFICATION(prompt, LLM) ... messageuser ← 'Please revise the following sentence with no change to its length and only output the revised version, the sentence is: prompt' return LLM.get_response(messagesystem, messageuser)\"."
}
```