```json
{
  "value": "High",
  "why": "The paper constructs and mutates inputs at the textual/token level (e.g., by paraphrasing with GPT-4, appending adversarial suffixes, using GCG, and prompt-rewriting) rather than applying small in-memory floating-point perturbations to internal/normalized representations. Text prompts are produced as serialized strings (discrete tokens), so fault-inducing perturbations are inherent in the serialized input format and will survive standard serialization. The paper repeatedly describes generating/editing query strings (synonyms, adversarial suffixes, GPT-4 rewrites) and analyzing token-level behavior, indicating that input-format constraints for text are respected.",
  "evidence": "Page 2, Section II-A (Model Inference Process): \"The text is tokenized into discrete elements, each mapped to a dense embedding vector representing token semantics for further processing by the model.\" \n\nPage 3, Section III-A (Motivation: A Cluster Analysis Experiment): \"We collect four distinct datasets and use 200 queries from each dataset... ❹ Attack queries are generated by appending adversarial suffixes to rejected queries using GCG [7]. These queries aim to trigger the model to output malicious content (i.e., abnormal behaviors).\" \n\nPage 6, Section III-A (Test Suite Construction / Synonymous Query Dataset): \"To construct synonymous queries, we use GPT-4 to generate corresponding synonymous paraphrases for the first 500 queries from the Alpaca-gpt4 dataset and manually verify that the synonymous queries produce outputs similar to the original queries...\" \n\nPage 11, Section V-C (Jailbreak Case Generation): \"Over five iterations, GPT-4 generates ten new jailbreak queries per round through prompt rewriting. The query with the highest coverage increase is selected as the next seed in the coverage-guided approach.\""
}
```