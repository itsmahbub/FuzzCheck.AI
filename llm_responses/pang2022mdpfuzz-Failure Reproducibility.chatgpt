```json
{
  "value": "High",
  "why": "MDPFuzz mutates inputs at the MDP/environment state level (raw, simulator-state values) rather than in normalized/latent intermediate representations. The authors explicitly constrain mutations to legitimate state-space ranges, apply small, realistic perturbations (e.g., Gaussian floats to vehicle positions, bounded speed ranges), and validate mutated initial states using the environment’s validation modules to ensure realism and solvability. Because inputs to the tested models are produced by the environment (e.g., rendered frames or validated numeric state vectors) rather than by directly altering normalized internal tensors, fault-inducing perturbations are preserved through the environment/serialization pipeline. These choices correspond to the metric’s “High” definition: mutations respect input-format constraints or generate inputs via the environment/serialized format.",
  "evidence": "Page 381, Section 4 (Testing DNN Models Solving MDPs): \"MDPFuzz only mutates the initial state conservatively to ensure that the sequence is realistic. The mutated initial states are also validated in a deliberate way to ensure realism.\" \n\nPage 382, Section 5 (Design) — States Validation: \"Seed mutation is bounded in the legitimate state space of MDP to guarantee that such initial states exist and solvable in real-world scenarios. We also use the validation module shipped by some MDP environments to validate the mutated states. In short, we clarify that all the mutated initial states are valid and solvable...\" \n\nPage 385, Section 6 (MDP Initial State Sampling, Mutation, and Validation): \"We add random noise to the initial states and the noise type depends on type of the original data, e.g., adding small Gaussian float numbers from N(0, 1) to vehicles’ initial positions, and adding small uniform integers from U(0, 4) to the ground type in BipedalWalker... Note that CARLA validates and rejects abnormal initial states: all mutated initial states passed its validation. ... In DNN-based ACAS Xu, we mutate the initial positions and speeds of the model-controlled and the other planes. Moreover, we bound the maximal speed of all airplanes below 1,100 ft/sec... We guarantee that there exist optimal actions to avoid the crash and solve the initial states, and we do not use an initial state that is not solvable.\""
}
```