```json
{
  "value": "High",
  "why": "The fuzzer (DeepEvolution) is explicitly designed to generate semantically-preserving, realistic inputs. The authors (1) state the approach uses 'semantically-preserving metamorphic transformations' (design assumption), (2) manually tune valid domain intervals (high/low boundaries) for each transformation to preserve input semantics with respect to the data distribution, (3) enforce a conservative transformation strategy (only one affine after pixel transforms) to avoid generating meaningless images, and (4) verify semantic equivalence by computing SSIM and rejecting mutants below a tuned threshold. These measures constrain mutations relative to the original seed and restrict the search to plausible transformations, addressing both semantic consistency and naturalness across iterations.",
  "evidence": "Page 454, Abstract: \"DeepEvolution relies on population-based metaheuristics to explore the search space of semantically-preserving metamorphic transformations.\" \n\nPage 455, Section IV.A (Metamorphic Transformation): \"Because each image-based transformation has a theoretical domain, which defines the interval of possible values of its parameters... when applying transformations, we need to take into account these domain boundaries to ensure that transformed inputs are semantically equivalent to the original ones. To infer the valid domain interval of each transformation’s parameters, we manually tune them to set up the appropriate range of values, i.e., high and low boundaries, that preserves the input semantics before and after each transformation, with respect to the data distribution.\" \n\nPage 455, Section IV.A: \"We opted for this conservative strategy that consists of applying only one affine transformation following the pixel-value transformations because applying multiple affine transformations at once would increase the chances of generating meaningless images, i.e., images that don’t occur in real-world situations.\" \n\nPage 456, Section IV.A: \"To verify that generated inputs remain semantically equivalent to the original inputs, we compute a Structural Similarity Index (SSIM)... Therefore, we reject mutated synthetic inputs for which SSIM values are lower than a tuned threshold.\" \n\nPage 456, Section IV.C (Swarm-based Metaheuristics): \"To ensure semantically preserving transformations, we use the valid domain intervals of transformations that we have already tuned manually to create the high and low boundaries vectors, defining the sub-space of exploration.\""
}
```