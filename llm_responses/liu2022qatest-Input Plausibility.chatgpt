```json
{
  "value": "Medium",
  "why": "QATest enforces semantic consistency and naturalness checks (ROUGE-1 quality threshold and a perplexity-based priority) but these checks are applied relative to the current seed / current iteration and selected batch rather than being constrained with respect to the original seed across multiple iterations. The algorithm computes ROUGE-1 between the current seed and its immediate mutant and uses a perplexity priority computed from the (dynamically updated) seed set — mechanisms that preserve plausibility step-by-step but do not explicitly prevent cumulative semantic drift over many iterative mutations.",
  "evidence": "Page 4, Section 3.1 (Algorithm 1): \"quality_score ← Rouge1(s, s') ; if quality_score > τ then D.append(s')\" — (QATest uses ROUGE-1 to accept mutants based on similarity to the immediate seed).\n\nPage 5, Section 3.2.5 (Quality Assessment): \"To ensure the quality of generated questions, we employ the ROUGE-1 metric ... for a given original question q and the corresponding transformed question q' , the ROUGE-1 score can be calculated as follows: ...\" — (quality is evaluated between the seed and its transformed mutant).\n\nPage 6, Section 3.3.2 (Perplexity Priority): \"...we design a perplexity priority criterion ... the perplexity of the abstract sequence seq(q) can be represented as: per(q) = p(seq(q))^{-1/n} ... s** = arg min_{q in G} per(q)\" — (naturalness/authenticity is enforced via perplexity computed from the current seed set and used to select mutants to feed back).\n\nPage 6, Section 3.3.1 (N-Gram Coverage) and Definition 3.2: \"H(q) = {s | s != q ∧ ori(s) = ori(q) ∧ s ∈ S}\" and cov(q) = |k-Gram(q) − ∪_{s∈H(q)}{k-Gram(s)}| / |∪_{s∈H(q)}{k-Gram(s)}| — (diversity/coverage is measured relative to homologous variants in the current seed set, not enforced against the original seed across iterations).\n\nPage 6, Section 3.4 (Seed Selection Strategy): \"we record the original seed data corresponding to each question ... prefer to select seeds with few variants for further transformation\" — (the framework biases seed selection to maintain diversity but does not describe a mechanism that controls cumulative semantic drift relative to the original seed)."
}
```