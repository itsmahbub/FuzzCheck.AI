```json
{
  "value": "High",
  "why": "The paper generates perception-based mutations by synthesizing mutated images through generative models (GANs) — i.e., mutations are produced as rendered images via G(z+v) rather than as tiny in-memory floating-point perturbations to normalized pixel arrays. These generated images are therefore serialized (rendered) outputs that inherently respect image storage formats, avoiding the class of serialization-rounding/clipping losses described in the metric. The authors also validate the naturalness/validity of the mutated images via a human study, supporting that the synthesized images are realistic serialized inputs rather than ephemeral in-memory perturbations.",
  "evidence": "Page 1608, Section 4.1 \"Editing Perceptual Properties\": \"Perception-based transformations are implemented based on generative models (e.g., GANs [20]). Given a generative model G and a latent vector z (which can be random or converted using a real image), editing the generated image G(z) can be characterized using the Taylor expansion... existing works decompose J(z) into orthogonal vectors V = {v1, v2, ...}, and each vi ∈ V corresponds to a unique perception-based transformation ti, i.e., ti(x) = G(z + vi), and the norm of vi decides the extent of the mutation achieved by ti.\" \n\nPage 1608, Eq. (4) (Section 4.1 Local-Perception Transformation): \"x' = G(z + B B^T v).\" (shows mutations are applied in latent space and rendered through G)\n\nPage 1609, Section 5 \"Pilot Study\" (Validity): \"Validity of Mutated Images. We do not observe invalid images (i.e., an image is 'unnatural' and hardly recognized by humans [12,45]) generated by our transformations. In fact, the validity of these mutations are ensured by Jacobian matrix decomposition in a principled manner [6, 61, 72, 73]; see more samples in our artifact [2].\"\n\nPage 1610, Section 6 \"Augmented Transformations\": \"Due to the overhead and efficacy consideration, we use GANs to generate perception-based transformations... We use BigGAN [6] trained with ImageNet to generate transformations for different classes in ImageNet... StyleGANs [32] are adopted to generate transformations for the remaining domain-specific datasets... In Sec. 7 and Sec. 8, our evaluations require transformations that can trigger DNN mis-predictions. For ImageNet DNNs, we generate 1944 perception-based transformations that can trigger mis-predictions on all of them.\""
}
```