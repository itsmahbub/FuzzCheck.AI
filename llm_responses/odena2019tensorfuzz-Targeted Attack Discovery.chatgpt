```json
{
  "value": "High",
  "why": "TensorFuzz is designed to accept arbitrary, user-defined objective functions that directly encode the property or attacker-chosen goal to steer the search toward. The paper both explains this design (Objective Function / property-based testing) and demonstrates it in experiments where the fuzzer was steered to specific predefined properties (e.g., inputs that produce non-finite/NaN values and inputs that cause prediction disagreements between 32-bit and 16-bit quantized models). These constitute targeted discovery of attacker-chosen properties rather than untargeted failures or only broad classes of unsafe behavior.",
  "evidence": "Page 1, Abstract: \"coverage-guided fuzzing (CGF) methods for neural networks. In CGF, random mutations of inputs are guided by a coverage metric toward the goal of satisfying user-specified constraints.\"; Page 5, Section 'Objective Function': \"we allow for arbitrary user-defined goals. When the mutated inputs are fed into the computation graph, both coverage arrays and metadata arrays are returned as output. The objective function is applied to the metadata arrays, and flags inputs that caused the objective to be satisfied. In this way, the objective function asserts the property to be tested and the fuzzer attempts to find a violation of this property.\"; Page 7, Section 'TensorFuzz can efficiently find numerical errors in trained neural networks': \"With CGF, we should be able to simply add operations to the metadata that check for non-finite elements and then run our fuzzer... TensorFuzz found NaNs quickly across 10 different random initializations.\"; Page 7, Section 'TensorFuzz surfaces disagreements between models and their quantized versions': \"...the fuzzer was able to generate disagreements for 70% of the examples we tried. Thus, CGF allowed us to find real errors that could have occured at test time.\""
}
```