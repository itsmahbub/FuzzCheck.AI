```json
{
  "value": "High",
  "why": "DeepXplore uses differential oracles (cross-referencing multiple DNNs), so semantic-consistency requirements (metamorphic assumptions) are not applicable. For the dimension that is applicable — naturalness — the paper explicitly enforces domain-specific realism constraints at every gradient-ascent iteration (Algorithm 1 calls DOMAIN_CONSTRNTS on the gradient and the text states the algorithm modifies the gradient so each xi+1 satisfies the domain constraints). The paper gives concrete, per-iteration constraints for images (only uniform brightness changes, bounded occlusion patches, limited patch rules) and for file/manifest features (only add manifest features, do not remove functionality), and describes enforcing these constraints throughout the iterative mutation process. These design choices demonstrate that DeepXplore enforces naturalness across fuzzing iterations, meeting the metric’s requirement for High.",
  "evidence": "Page 3 (Overview): \"we leverage multiple DL systems with similar functionality as cross-referencing oracles to avoid manual checking.\" / Section 3, para 2. (Shows DeepXplore uses differential oracles.)\n\nPage 5 (Overview / Figure 5 caption): \"DeepXplore also supports enforcing of custom domain-specific constraints as part of the joint optimization process. For example, the value of an image pixel has to be between 0 and 255. Such domain-specific constraints can be specified by the users of DeepXplore to ensure that the generated test inputs are valid and realistic.\" / Section 3, para 3.\n\nPage 7 (Algorithm / Domain-specific constraints): Algorithm 1 line 13: \"grad = DOMAIN_CONSTRNTS(grad)\" and main text: \"our algorithm must ensure that after i-th (i > 0) iteration of gradient ascent, xi still satisfies the constraints. Our algorithm ensures this property by modifying the gradient дrad (line 13 in Algorithm 1) such that xi+1 = xi + s · дrad still satisfies the constraints (s is the step size in the gradient ascent).\" / Section 4.2, 'Domain-specific constraints'.\n\nPage 9 (Domain-specific constraints — concrete rules): \"The first constraint restricts image modifications so that DeepXplore can only make the image darker or brighter without changing its content. Specifically, the modification can only increase or decrease all pixel values by the same amount... The second constraint simulates the effect of the camera lens that may be accidentally or deliberately occluded by a single small rectangle... The third constraint restricts the modifications so that DeepXplore only selects a tiny m x m size patch...\" / Section 6.2 'Domain-specific constraints'.\n\nPage 10 (Domain constraints for malware features): \"For Drebin dataset, DeepXplore enforces a constraint that only allows modifying features related to the Android manifest file and thus ensures that the application code is unaffected. Moreover, DeepXplore only allows adding features (changing from zero to one) but do not allow deleting features (changing from one to zero) from the manifest files... after computing the gradient, DeepXplore only modifies the manifest features whose corresponding gradients are greater than zero.\" / Section 6.2, 'Other constraints (Drebin and Contagio/VirusTotal)'."
}
```