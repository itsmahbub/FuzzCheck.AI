```json
{
  "value": "Low",
  "why": "The paper's KuK test generator performs pixel-level crossover and adds continuous-valued (floating-point) noise to images under an L∞ perturbation budget (radius = 0.3) inside the in-memory processing pipeline (Keras/TensorFlow). The method descriptions and implementation notes show mutations are applied in continuous image space (e.g., exchanging pixels, adding white noise, L∞ bound) and there is no discussion or mechanism described to enforce serialized input-format constraints (integer pixel rounding, clipping to 0..255 on disk, or saving images as PNG/JPEG) or to ensure perturbations survive denormalization/serialization. Therefore the approach mutates in a continuous intermediate representation without enforcing format constraints, matching the metric’s Low category.",
  "evidence": "Section 4, Population Initialization (page 746): \"Population Initialization. Given an input image, we first generate a set of images by randomly adding noise to it... We use L∞ norm to constrain the allowable changes between the original seed and its generated counterpart.\" \n\nSection 4, Crossover and Mutation (page 746): \"The two selected images are used to perform the crossover by randomly exchanging the corresponding pixels. After the crossover, each image is randomly mutated by adding white noise, to increase the diversity of the population.\" \n\nSection 5.1, Setting (page 746–747): \"In the genetic algorithm, the size of the population is set as 100, the crossover rate is set as 0.5 and the mutation rate is 0.005. For the mutation process, the radius of L∞ is set as 0.3.\" \n\nSection 5, Implementation (page 746–747): \"We implemented the proposed test generation tool, KuK, in Python based on Keras (2.2.4) with TensorFlow (1.12.0) as backend.\" \n\n(There is no text in the paper describing clipping/rounding to serialized image formats or any explicit handling of integer pixel ranges or image file encoding to ensure perturbations survive serialization.)"
}
```