```json
{
  "value": "High",
  "why": "The paper explicitly designs DRFuzz to preserve both semantic consistency and naturalness across fuzzing iterations. It enforces naturalness via a GAN-based fidelity discriminator (used as a per-seed fidelity threshold to filter out low-fidelity mutants), enforces semantic-preserving mutation rules (pixel-level mutations limited to 0.5% of pixels and image-level rules applied to whole images), and includes mechanisms to limit cumulative drift across iterations (a per-seed max_times and an exponential decay selection probability to reduce repeated mutation of the same seed). These mechanisms control realism relative to the original seeds rather than only relative to the immediately previous mutant, satisfying the High definition.",
  "evidence": "III-B GAN-based Fidelity Assurance (page 84): \"To mitigate the limitations of existing methods, we adopt Generative Adversarial Network (GAN) for fidelity assurance... For each given input, the well-trained Discriminator can output a score in the range of [0, 1], where a higher score indicates the given input resembles the training set with higher fidelity. During the fuzzing process, we feed an initial seed into the Discriminator to get a fidelity score, which serves as the fidelity threshold for filtering out the low-fidelity inputs generated from this initial seed.\" \n\nIII-C1 Mutation (page 85): \"Besides generating diverse fault-triggering test inputs, another criterion for mutation rules is to preserve the semantics of a test input after mutation... Pixel-Level Mutation aims to change a small set of pixels in an image to generate a new test input. DRFuzz randomly selects a small set of pixels (i.e., 0.5% total pixels) for mutation. Image-Level Mutation aims to change the whole image to generate a new test input...\" \n\nIII-C2 Seed Maintenance / Seed Probability Update (page 86): \"max_times is the maximum number of times a seed can be mutated, which can limit the number of similar seeds and avoid significant changes causing semantic deviations from the initial seed to some degree.\" \n\nAbstract / Introduction (page 83): \"To obtain test inputs with high fidelity, DRFuzz designs a GAN-based fidelity assurance method, which first learns a discriminator via GAN to discriminate natural inputs with synthetic inputs precisely and then utilizes it to ensure that the generated test inputs resemble natural inputs in the fuzzing process.\""
}
```