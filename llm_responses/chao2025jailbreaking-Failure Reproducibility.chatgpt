```json
{
  "value": "High",
  "why": "The method generates adversarial prompts directly as serialized text inputs (token/JSON strings) and sends them via black-box API queries to the target LLM. PAIR does not perform mutations in continuous/normalized or latent spaces; instead it constructs textual prompts (including JSON-formatted attacker outputs) that are passed verbatim to the target, so there is no risk of fault-inducing perturbations being lost during serialization.",
  "evidence": "Page 3, Section III-B (Algorithm 1): \"Sample P ∼ qA(C)\" and \"Sample R ∼ qT(P)\" (algorithm steps show prompts P are sampled/generated by the attacker and then passed as input to the target).; Page 15 (Appendix B, Table XII system prompt template): \"The language model receives **only your prompt P as input** and has no knowledge of the chat history. **Your objective is to craft an adversarial jailbreaking prompt P which you continuously iterate and refine, where when P is entered to a language model, the model performs the following behavior...\" (shows prompts are text inputs entered to the model).; Page 4, Section IV (Target LLMs / Evaluation): \"Since PAIR only requires black box access, we use public APIs for all of our experiments\" (indicates prompts are sent as serialized API/text inputs rather than operating on internal continuous representations)."
}
```