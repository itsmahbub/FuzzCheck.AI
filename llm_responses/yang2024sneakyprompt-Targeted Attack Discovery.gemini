```json
{
  "value": "High",
  "why": "The proposed method, SneakyPrompt, is designed to discover attacks that achieve a specific, attacker-chosen goal. The framework takes a 'sensitive, target prompt' (pt) that is blocked by a safety filter and aims to find an adversarial prompt (pa) that not only bypasses the filter but also generates an image with 'similar visual semantics' to the original target. The entire optimization process, including the objectives and the reward function for the reinforcement learning agent, is guided by preserving the semantics of the specific target prompt. This steering towards a predefined output (an image that visually matches the intent of a specific sensitive prompt) aligns with the 'High' level of the metric, as it targets a specific, predefined goal rather than just any general safety-violating behavior.",
  "evidence": "Page 3, Section 3.1, Definition 1. [Adversarial Prompt]: \"A prompt to a text-to-image model M is an adversarial prompt pa relatively to a sensitive, target prompt pt (i.e., F(M, pt) = 1), if F(M, pa) = 0 and M(pa) has similar visual semantics as M(pt).\"\n\nPage 5, Section 4.1, Objective I: \"Objective I: Searching for a prompt with target semantic. That is, M(pa) has the same sensitive semantics as the target prompt pt.\""
}
```