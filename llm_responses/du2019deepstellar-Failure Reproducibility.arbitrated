```json
{
  "value": "Medium",
  "why": "DeepStellar’s test‐generation mutators operate directly on raw input values (e.g. image contrast, brightness, rotation; audio speed, volume, filtering, noise) without any mention of enforcing serialized‐format constraints (such as clipping pixels to [0,255] or rounding to integer sample amplitudes). Although the adversarial detection uses TTS to produce reference audio in a serialized form, the core fuzzing and perturbation steps apply in‐memory transformations with no guarantee that perturbations survive standard serialization—matching the ‘Medium’ category of direct raw‐value mutations without explicit format enforcement.",
  "evidence": "Page 482, Section 5.2: “Randomly pick transformation t with a random parameter p; A = mutate(t, p, a); … apply the metamorphic mutation strategy [55] for generating new mutants.”\nPage 483, Section 6.1: “For each ASR model, we generate 10,000 perturbed audios … with existing audio data augmentation techniques [5] (i.e., speed and volume adjustment, low/high-frequency filtering, noise blending). … For each MNIST model, we also generate 10,000 benign perturbed samples with existing image transformation techniques [50] (i.e., image contrast, brightness, translation, scaling, shearing, rotation and add white noise).”"
}
```