```json
{
  "value": "Low",
  "why": "The paper describes applying metamorphic image transformations via continuous, bounded real-valued parameter vectors optimized by metaheuristics “without any prior search space discretization” (IV.C). Transformations are defined and tuned as continuous parameter ranges (IV.A) and applied in-memory to produce mutated inputs, with semantic checks (SSIM) to reject overly-distorted images. The paper does not describe enforcing serialization-format constraints (e.g., clipping/rounding to 8-bit [0,255] pixel integers) or generating inputs directly in a serialized image format; instead it operates on continuous transformation parameters and produced images in-memory. Therefore the method mutates in a continuous intermediate representation without explicit enforcement of format constraints that would guarantee perturbations survive serialization, matching the metric’s “Low” definition.",
  "evidence": "Page 2, Section IV.A (Metamorphic Transformation): \"To infer the valid domain interval of each transformation’s parameters, we manually tune them to set up the appropriate range of values, i.e., high and low boundaries, that preserves the input semantics before and after each transformation, with respect to the data distribution.\"; Page 2, Section IV.A: \"... applying the supported pixel-value transformations in sequence, and then, performing each single affine transformation once, on the resulting mutated input.\"; Page 3, Section IV.C (Swarm-based Metaheuristics): \"We encode our compound metamorphic transformations as a vector, where each component represents one parameter ... These metaheuristics algorithms are flexible enough to be easily applicable to a broad class of constrained optimization problems involving high dimensional bounded real-valued vectors without any prior search space discretization.\" (No section or quote in the paper describes clipping/rounding to integer pixel ranges or producing serialized image formats such as PNG/JPEG.)"
}
```