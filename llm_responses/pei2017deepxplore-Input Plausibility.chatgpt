```json
{
  "value": "High",
  "why": "DeepXplore explicitly designs and enforces domain-specific constraints that preserve semantic content and realism during iterative mutation (e.g., only changing overall brightness, using occlusion patches, only adding manifest features rather than removing them, clamping pixels to valid ranges). The paper further states that the gradient updates are modified so that each iteration xi+1 = xi + s·grad still satisfies the domain constraints, which prevents mutations from producing semantically invalid or unrealistic inputs across fuzzing iterations. These mechanisms both (1) fix or limit semantically critical content (e.g., keep image content unchanged when only simulating lighting, restrict manifest edits to additions) and (2) enforce naturalness/realism (e.g., pixel range, occlusion shapes, feature restriction), matching the High criterion.",
  "evidence": "Section 3 (Overview), Page 5–6: \"DeepXplore also supports enforcing of custom domain-specific constraints as part of the joint optimization process. For example, the value of an image pixel has to be between 0 and 255. Such domain-specific constraints can be specified by the users of DeepXplore to ensure that the generated test inputs are valid and realistic.\"; Section 4.2 (Domain-specific constraints), Page 8: \"One important aspect of the optimization process is that the generated test inputs need to satisfy several domain-specific constraints to be physically realistic ... our algorithm ensures this property by modifying the gradient ... such that xi+1 = xi + s · grad still satisfies the constraints (s is the step size in the gradient ascent).\"; Section 6.2 (Domain-specific constraints), Page 9–11: \"The first constraint restricts image modifications so that DeepXplore can only make the image darker or brighter without changing its content... The second constraint simulates the effect of the camera lens ... by a single small rectangle ... The third constraint restricts the modifications so that DeepXplore only selects a tiny m×m size patch ... For Drebin dataset, DeepXplore enforces a constraint that only allows modifying features related to the Android manifest file ... DeepXplore only allows adding features (changing from zero to one) but do not allow deleting features (changing from one to zero) ...\""
}
```